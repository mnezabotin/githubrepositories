import styled from 'styled-components'

export const Input = styled.input`
  font-family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif;
  border-radius: 0.3rem;
  border: 2px solid #000000;
  padding: 0.3rem 1rem;
  font-size: 0.8rem;
  font-weight: 500;

  :focus,
  :focus-visible {
    outline: 4px auto #5a8dee;
  }
`
